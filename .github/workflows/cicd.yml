name: Taller CI/CD
run-name: Estamos ejecutando acciones automáticas

on: [push]

jobs:
  MiPrimeraAutomatizacion:
    runs-on: ubuntu-latest
    steps:
      - name: Cargar los ficheros de mi repo
        uses: actions/checkout@v6
      - name: Vemos el contenido de la carpeta
        run: ls -luah .
      